apiVersion: v1
kind: Service
metadata:
  name: {{ include "classquiz.fullname" . }}-redis
  labels:
    {{- include "classquiz.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "classquiz.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis